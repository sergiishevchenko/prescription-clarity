# Local dev (npm run dev): uses Postgres exposed on localhost:5432
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/goit?schema=public"

# Shared app settings
SESSION_COOKIE_NAME="SESSION_ID"
SESSION_SECRET="replace-with-long-random-string"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Docker dev (docker compose): app connects to service hostname `db`
# DATABASEA_URL="postgresql://postgres:postgres@db:5432/goit?schema=public"
